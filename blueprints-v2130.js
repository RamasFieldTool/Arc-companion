(()=>{const KEY='arc_blueprints_learned_v1';let data=[],filter='all';const learned=new Set(JSON.parse(localStorage.getItem(KEY)||'[]'));const $=id=>document.getElementById(id);const lang=()=>localStorage.getItem('arcLang')||document.documentElement.lang||'de';const T={de:{all:'ALLE',learned:'GELERNT',missing:'FEHLT',plan:'Bauplan',learnedWord:'gelernt',missingWord:'fehlen',shown:'angezeigt',saved:'Fortschritt wird auf diesem Gerät gespeichert',none:'Keine Treffer.',load:'Bauplan-Daten konnten nicht geladen werden.',info:'INFO / FUNDORT',map:'Karte',condition:'Bedingung',container:'Container',quest:'Quest',trials:'Trials',unknown:'Nicht bestätigt',yes:'Ja',no:'Nein',community:'COMMUNITY-DATEN',confirmed:'BESTÄTIGT',trialRandom:'Zufällige Trial-Belohnung möglich; kein garantierter Blueprint.',lootWarning:'Map-/Containerangaben sind Community-Daten und keine garantierten Spawnpunkte.'},en:{all:'ALL',learned:'LEARNED',missing:'MISSING',plan:'Blueprint',learnedWord:'learned',missingWord:'missing',shown:'shown',saved:'Progress is saved on this device',none:'No results.',load:'Blueprint data could not be loaded.',info:'INFO / LOCATION',map:'Map',condition:'Condition',container:'Container',quest:'Quest',trials:'Trials',unknown:'Not confirmed',yes:'Yes',no:'No',community:'COMMUNITY DATA',confirmed:'CONFIRMED',trialRandom:'Random Trial reward possible; this blueprint is not guaranteed.',lootWarning:'Map/container information is community data, not a guaranteed spawn location.'}};const tr=k=>(T[lang()]||T.de)[k];function save(){localStorage.setItem(KEY,JSON.stringify([...learned]));}function primary(x){return lang()==='en'?(x.name_en||x.name_de||x.name||x.id):(x.name_de||x.name_en||x.name||x.id);}function secondary(x){const p=primary(x),alt=lang()==='en'?(x.name_de||''):(x.name_en||'');return alt&&alt!==p?alt:tr('plan');}function v(x,k){const z=x?.acquisition?.[k];if(z===true)return tr('yes');if(z===false)return tr('no');return z||tr('unknown');}function info(x){const a=x.acquisition||{};const has=Object.keys(a).length>0;const confidence=a.confidence==='confirmed'?tr('confirmed'):a.confidence==='community'?tr('community'):tr('unknown');return `<details class="blueprint-info" data-id="${x.id}"><summary>${tr('info')} · ${confidence}</summary><div class="blueprint-info-grid"><div><b>${tr('map')}</b><span>${has?v(x,'map'):tr('unknown')}</span></div><div><b>${tr('condition')}</b><span>${has?v(x,'condition'):tr('unknown')}</span></div><div><b>${tr('container')}</b><span>${has?v(x,'container'):tr('unknown')}</span></div><div><b>${tr('quest')}</b><span>${has?v(x,'quest'):tr('unknown')}</span></div><div><b>${tr('trials')}</b><span>${has?v(x,'trials'):tr('unknown')}</span></div></div>${a.trials===true?`<p class="blueprint-caution">${tr('trialRandom')}</p>`:''}${a.confidence==='community'?`<p class="blueprint-caution">${tr('lootWarning')}</p>`:''}</details>`;}function render(){const q=($('blueprintQ')?.value||'').trim().toLowerCase();const rows=data.filter(x=>{const a=x.acquisition||{};const hay=[x.name_de,x.name_en,x.name,x.station,x.type,a.map,a.condition,a.container,a.quest].filter(Boolean).join(' ').toLowerCase();return(!q||hay.includes(q))&&(filter==='all'||(filter==='learned')===learned.has(x.id));});$('blueprintList').innerHTML=rows.map(x=>`<div class="blueprint-entry ${learned.has(x.id)?'is-learned':''}"><button class="blueprint-row ${learned.has(x.id)?'is-learned':''}" data-id="${x.id}" type="button"><span class="blueprint-check">${learned.has(x.id)?'✓':'○'}</span><span><b>${primary(x)}</b><small>${secondary(x)}</small></span></button>${info(x)}</div>`).join('')||`<p class="muted">${tr('none')}</p>`;const validIds=new Set(data.map(x=>x.id));const n=[...learned].filter(id=>validIds.has(id)).length;$('blueprintSummary').textContent=`${n} / ${data.length} ${tr('learnedWord')} · ${Math.max(0,data.length-n)} ${tr('missingWord')}`;$('blueprintStatus').textContent=`${rows.length} ${tr('shown')} · ${tr('saved')}`;document.querySelectorAll('.blueprint-filter').forEach(b=>{b.textContent=tr(b.dataset.filter);b.classList.toggle('active',b.dataset.filter===filter)});document.querySelectorAll('.blueprint-row').forEach(b=>b.onclick=()=>{learned.has(b.dataset.id)?learned.delete(b.dataset.id):learned.add(b.dataset.id);save();render();});}async function init(){try{const [bp,acq]=await Promise.all([fetch('blueprints.json?v=2130a').then(r=>{if(!r.ok)throw Error(r.status);return r.json()}),fetch('blueprint-acquisition-v2130.json?v=2130a').then(r=>r.ok?r.json():({}))]);data=bp.map(x=>({...x,acquisition:acq[x.id]||null}));$('blueprintQ').oninput=render;document.querySelectorAll('.blueprint-filter').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;render();});$('deBtn')?.addEventListener('click',()=>setTimeout(render,0));$('enBtn')?.addEventListener('click',()=>setTimeout(render,0));render();}catch(e){$('blueprintStatus').textContent=tr('load');}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();
(()=>{const KEY='arc_blueprints_learned_v1';let data=[],filter='all';const learned=new Set(JSON.parse(localStorage.getItem(KEY)||'[]'));const $=id=>document.getElementById(id);function save(){localStorage.setItem(KEY,JSON.stringify([...learned]));}function render(){const q=($('blueprintQ')?.value||'').trim().toLowerCase();const rows=data.filter(x=>(!q||x.name.toLowerCase().includes(q)||x.station.toLowerCase().includes(q))&&(filter==='all'||(filter==='learned')===learned.has(x.id)));$('blueprintList').innerHTML=rows.map(x=>`<button class="blueprint-row ${learned.has(x.id)?'is-learned':''}" data-id="${x.id}" type="button"><span class="blueprint-check">${learned.has(x.id)?'✓':'○'}</span><span><b>${x.name}</b><small>${x.station} · ${x.type.toUpperCase()}</small></span></button>`).join('')||'<p class="muted">Keine Treffer.</p>';const n=learned.size;$('blueprintSummary').textContent=`${n} / ${data.length} gelernt · ${Math.max(0,data.length-n)} fehlen`;$('blueprintStatus').textContent=`${rows.length} angezeigt · Fortschritt wird auf diesem Gerät gespeichert`;document.querySelectorAll('.blueprint-row').forEach(b=>b.onclick=()=>{learned.has(b.dataset.id)?learned.delete(b.dataset.id):learned.add(b.dataset.id);save();render();});}async function init(){try{data=await fetch('blueprints.json?v=2130').then(r=>{if(!r.ok)throw Error(r.status);return r.json()});$('blueprintQ').oninput=render;document.querySelectorAll('.blueprint-filter').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;document.querySelectorAll('.blueprint-filter').forEach(x=>x.classList.toggle('active',x===b));render();});render();}catch(e){$('blueprintStatus').textContent='Bauplan-Daten konnten nicht geladen werden.';}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();
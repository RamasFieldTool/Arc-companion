(()=>{const KEY='arc_blueprints_learned_v1';let data=[],filter='all';const learned=new Set(JSON.parse(localStorage.getItem(KEY)||'[]'));const $=id=>document.getElementById(id);function save(){localStorage.setItem(KEY,JSON.stringify([...learned]));}function label(x){return x.name_de||x.name_en||x.name||x.id;}function sublabel(x){const en=x.name_en||x.name||'';return x.name_de&&en&&x.name_de!==en?en:'';}function render(){const q=($('blueprintQ')?.value||'').trim().toLowerCase();const rows=data.filter(x=>{const hay=[x.name_de,x.name_en,x.name,x.station,x.type].filter(Boolean).join(' ').toLowerCase();return(!q||hay.includes(q))&&(filter==='all'||(filter==='learned')===learned.has(x.id));});$('blueprintList').innerHTML=rows.map(x=>`<button class="blueprint-row ${learned.has(x.id)?'is-learned':''}" data-id="${x.id}" type="button"><span class="blueprint-check">${learned.has(x.id)?'✓':'○'}</span><span><b>${label(x)}</b><small>${sublabel(x)||'Bauplan'}</small></span></button>`).join('')||'<p class="muted">Keine Treffer.</p>';const validIds=new Set(data.map(x=>x.id));const n=[...learned].filter(id=>validIds.has(id)).length;$('blueprintSummary').textContent=`${n} / ${data.length} gelernt · ${Math.max(0,data.length-n)} fehlen`;$('blueprintStatus').textContent=`${rows.length} angezeigt · Fortschritt wird auf diesem Gerät gespeichert`;document.querySelectorAll('.blueprint-row').forEach(b=>b.onclick=()=>{learned.has(b.dataset.id)?learned.delete(b.dataset.id):learned.add(b.dataset.id);save();render();});}async function init(){try{data=await fetch('blueprints.json?v=2130').then(r=>{if(!r.ok)throw Error(r.status);return r.json()});$('blueprintQ').oninput=render;document.querySelectorAll('.blueprint-filter').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;document.querySelectorAll('.blueprint-filter').forEach(x=>x.classList.toggle('active',x===b));render();});render();}catch(e){$('blueprintStatus').textContent='Bauplan-Daten konnten nicht geladen werden.';}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();